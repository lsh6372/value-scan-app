# 修复三个开发规范问题

## 问题
1. **services.js 覆盖丢失** — 之前 write 整文件覆盖，导致 getTokenList API 丢失
2. **router.jsx 缺路由** — LongInflow 页面未注册路由
3. **App.jsx 首页被替换** — 直接渲染业务页面，绕过路由系统

## 修复
1. services.js：恢复 getTokenList + 新增 getLongInflowList（追加模式）
2. router.jsx：新增 `/long-inflow` → LongInflow 路由
3. App.jsx：恢复 BrowserRouter + useRoutes 路由体系，首页锁定 Home

## 规范记录
在 TOOLS.md 中写入四条开发纪律：
- 禁止覆盖共享聚合型文件
- 首页永远锁定 Home
- 修改前必须先 read
- 维护已有页面清单

## 结论
构建通过，三个问题已修复。