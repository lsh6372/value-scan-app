name = "valuescan-sse-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# 部署后访问地址：https://valuescan-sse-proxy.<你的子域>.workers.dev
# 或绑定自定义域名

# 环境变量（敏感信息，通过 wrangler secret 设置，不要写在这里）
# VALUESCAN_API_KEY = "ak_xxx"
# VALUESCAN_SECRET_KEY = "sk_xxx"
